reconnix_main_index:
    pattern:  /page/{name}
    defaults: { _controller: ReconnixMainBundle:Page:index }

reconnix_main_post:
    pattern:  /post/{tag}/{title}
    defaults: { _controller: ReconnixMainBundle:Post:index }